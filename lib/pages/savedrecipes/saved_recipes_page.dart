import 'package:flutter/material.dart';

class SavedRecipesPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('찜리스트'),
      ),
      body: Center(
        child: Text('여기에 찜 리스트 페이지 내용을 추가하세요'),
      ),
    );
  }
}